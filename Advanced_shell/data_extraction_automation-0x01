#!/bin/bash
jq -r '[.name, .types[0].type.name, .weight, .height]' data.json | awk -F',' '{printf "%s is of type %s, weighs %skg, and is %sm tall.\n", toupper(substr($1,2,1))tolower(substr($1,3)), toupper(substr($2,2,1))tolower(substr($2,3)), $3/10, $4/10}'